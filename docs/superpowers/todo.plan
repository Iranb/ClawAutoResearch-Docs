# TODO PLAN: 基于 Workflow Hooks 整合 top-tier evidence 架构

目标：
把已经实现的 top-tier evidence 子系统，按**当前代码真实架构**整合进 workflow。

核心原则：

1. **auto-iterator / stage-preflight 负责 materialize**
   - 负责生成 benchmark / stats / mechanism / repro / camera-ready 等 evidence artifacts
2. **workflow hooks 负责审核与放行**
   - 负责 reviewer file audit、revision dispatch、hook aggregate verdict
3. **不要把审核流程重新塞回 feature-specific auto loops**
   - 当前已经有 `tools/workflow-hooks/*`
   - 应该复用这层 durable hook，而不是再长一套“top-tier reviewer mini-framework”

4. **默认偏向非阻塞**
   - 除了真正影响科学有效性、可复现性、或提交完整性的节点，其他审核优先走 `warn_only` 或 `revise`，不要过早卡死 auto research

---

## 一、先基于当前代码重新判断边界

### 当前已存在的稳定 hook 能力

当前代码里已经有：

- hook contracts：
  - `tools/workflow-hooks/contracts.ts`
- hook executor：
  - `tools/workflow-hooks/executor.ts`
- file audit runner：
  - `tools/workflow-hooks/file-audit-runner.ts`
- reviewer polling loop：
  - `tools/workflow-hooks/runtime-review-loop.ts`
- hook gateway：
  - `tools/workflow-hooks/gateways.ts`
- hook runtime state：
  - `tools/workflow-hooks/state.ts`
- prompt/tool hook registration：
  - `tools/register-workflow-hooks.ts`

### 当前已接入的稳定 hook points

根据当前代码和文档，最关键的 hook points 是：

- `artifact_materialized`
- `before_stage_handoff`
- `before_task_complete`
- `before_handoff_activation`
- `after_handoff_activation`

### 当前 hook type 的现实约束

当前稳定支持的 hook type 只有：

- `file_audit`

这意味着：

- 本轮整合**不要优先发明新 hook type**
- 应先把 top-tier evidence 审核闭环落到 `file_audit` 上
- 通过：
  - requirement prompt
  - supporting artifacts
  - aggregate verdict
  - revision dispatch
  来控制审核流程

### 当前 top-tier evidence 已经实现但未完全接入 workflow 的模块

已经实现的新模块：

- `tools/research-contracts/*`
- `tools/research-intel/*`
- `tools/research-evidence/*`
- `tools/research-authoring/*`
- `tools/research-submit/*`

旧兼容入口：

- `tools/workflow-evidence/*`

当前最大缺口：

- 这些模块**还没有系统性接入**
  - `auto-iterator`
  - `stage-preflight`
  - `workflow tools`
  - `team task completion`
  - `before_stage_handoff` / `before_handoff_activation` hook gates

---

## 二、架构调整后的总原则

### 原则 1：不要让 hook 自己负责 evidence materialization

hook 的职责应该是：

- 审核新产物
- 决定 `pass / revise / block`
- 触发 revision dispatch

不应该让 hook 直接去：

- 聚合统计
- 写 benchmark registry
- 生成 repro pack

这些应该由：

- `auto-iterator`
- `stage-preflight`
- `workflow tool action`

先生成，再通过 hook 审。

### 原则 2：把 top-tier 项目的严格度收敛到 hook policy

当前 top-tier 逻辑已经部分依赖：

- `opportunity_scorecard.verdict = worth_top_tier_bet`

下一步不要继续在多个模块里 scattered `if worth_top_tier_bet`。

应该把“更严格”的部分尽量收敛成：

- 额外 materialization
- 额外 hook policy
- 更严格 handoff gate

### 原则 3：普通项目不应该被 top-tier hook 误伤

必须保证：

- 普通项目继续走轻量路径
- top-tier 项目才开启额外 evidence closeout + hook 审核

因此：

- hook policy 应该是 project-local、可 materialize 的
- 不应在全局默认强开

### 原则 4：区分“研究正确性 gate”和“投稿完备性 gate”

这是这次整合里最重要的放宽原则。

- 研究正确性 gate：
  - 可以在 experiment / analyze / review 边界阻塞
  - 因为这些问题会直接污染后续研究方向
- 投稿完备性 gate：
  - 尽量只在 write / submit 边界阻塞
  - 不要为了 camera-ready 细节去阻塞 experiment 或 analyze

### 原则 5：区分“值得 top-tier bet”和“允许继续推进”

`opportunity_scorecard` 应该影响：

- 审核强度
- hook policy 严格程度
- handoff 的文案和推荐动作

但不应该直接影响：

- 项目能不能继续做
- 普通 workflow 是否被中止

也就是说：

- `worth_top_tier_bet = no`
  - 应该降级为普通项目路径
  - 而不是硬 block

---

## 二点五、硬门 / 软门 / 提醒 的统一分级

后续整合时，所有新增审核点都必须先标等级，再决定接在哪个 hook point。

### A. 硬门：`block_stage`

只给下面这类问题：

1. 会直接破坏研究正确性
2. 会让后续阶段消费错误事实
3. 到 submit 时一定会被拒绝或失真

### B. 软门：`revise` + aggregate revision dispatch

给下面这类问题：

1. 不影响继续研究，但会降低 top-tier 竞争力
2. 需要回修，但不值得中断主流水线
3. 更适合在下一个 owner 那里修复

### C. 提醒：`warn_only`

给下面这类问题：

1. 只是质量提升建议
2. 对当前阶段不构成风险
3. 只影响“更强的稿”，不影响“是否能继续”

### D. 绝不直接阻塞

下面这类信息默认不应单独造成 block：

1. venue fit 不够理想
2. question 不够 sexy
3. mechanism evidence 还不够漂亮
4. camera-ready 美观性问题
5. 可以在 submit 前再补的产物

---

## 二点六、建议的审核分级矩阵

### 1. `benchmark_protocol`

建议等级：

- top-tier 项目：`block_stage`
- 普通项目：保留现有 deterministic signal，不额外挂 hook

原因：

- protocol lock 是研究正确性 gate
- 如果这里漂移，后面所有 compare 都可能失真

推荐 hook point：

- `artifact_materialized`
- `before_stage_handoff`（experiment -> analyze）

---

## 二点七、Handoff 质量审核的稳定覆盖计划

### 当前实现状态

- [x] 新增 `before_prepare_handoff` hook point
- [x] 新增 `before_handoff_delivery` hook point
- [x] 新增 `tools/workflow-hooks/handoff-gates.ts`
- [x] `research_workflow.prepare_stage_handoff` 创建 intent 前会运行 handoff hook gate
- [x] `createStageOwnerHandoffIntent(...)` 支持记录 hook gate fingerprint 到 intent payload
- [x] `deliverWorkflowHandoffIntent(...)` delivery 前会检查 hook gate freshness
- [x] `sweepPendingHandoffIntents(...)` 和 runtime maintenance retry 通过 delivery 层继承 freshness check
- [x] stale hook-gated handoff 会被 suppress / supersede，而不是继续投递
- [x] legacy auto code/gate/discussion loops 已通过 built-in hook bridge 暴露到 `workflow-hooks`
- [x] review-rounds 已把 pending/pass/revise/block 同步进 hook aggregate 体系
- [x] `completeWorkflowTaskAndContinue(...)` 已内置默认 `before_task_complete` hook gate

当前实现补充：

- legacy auto code review / submit readiness / auto mode discussion 会把 durable verdict 同步进 built-in hook state，再由 `before_stage_handoff` gateway 统一聚合
- `prepare_stage_handoff` 和显式 handoff gate 现在可以读取 built-in hook aggregate，而不只看 manifest 里的 `file_audit`
- review-round 创建时会先把状态标成 `auditing`，结果回写时再同步成 `pass / revise / block`
- `research_workflow.complete_task` 不再需要自己拼 `before_task_complete` callback，只需提供 hook context

目标：

把 handoff 相关的质量审核统一收敛到 `workflow-hooks` 控制平面，而不是继续让不同路径各自维护一套 review / gate / delivery 判断。

这里不按“最优先补哪个”排序。
这是一组需要整体覆盖的稳定实现面：

1. handoff intent 准备前
2. handoff intent 创建后、delivery 前
3. delivery / sweep / retry 前
4. handoff activation 前
5. task closeout 前
6. legacy review round / auto review loop 与 hook 的兼容迁移

### 当前已经 hook 化的边界

当前代码已经有这些 hook gateway：

- `artifact_materialized`
  - 来自 `stage-preflight`
  - 适合审核刚 materialize 的 workflow-owned artifacts
- `before_stage_handoff`
  - service 主循环在 `maybeLaunchAutoStageForProject(...)` 前调用
  - 适合阻止自动 owner handoff
- `before_task_complete`
  - `research_workflow.complete_task` 在 task closeout 前调用
  - 适合 section-level / task-level quality gate
- `before_handoff_activation`
  - handoff 被目标 owner claim 后、真正切换 `owner_agent/current_stage` 前调用
  - 适合作为 runtime final safety net
- `after_handoff_activation`
  - handoff 激活后调用
  - 适合记录、通知、非阻塞 audit，不适合回滚 owner/stage

这些点构成当前 hook control plane 的“已接入骨架”。

### 当前还未统一 hook 化的 handoff 路径

下面这些路径仍然没有完整进入 `workflow-hooks` 的 policy/filter/context/state/packet/revision-dispatch 体系：

1. `research_workflow.prepare_stage_handoff`
   - 当前会直接 `createStageOwnerHandoffIntent(...)`
   - 可选 `deliverWorkflowHandoffIntent(...)`
   - 没有在 intent 创建前统一跑 `before_stage_handoff` 或 `before_prepare_handoff`

2. `deliverWorkflowHandoffIntent(...)`
   - 当前主要处理 delivery budget、binding gate、runtime/lobster/mailbox delivery
   - 不重新检查 hook verdict freshness
   - 不会判断这个 handoff intent 是否已经被新的 hook state supersede

3. `sweepPendingHandoffIntents(...)`
   - 当前负责扫描可重试 handoff 并重新 delivery
   - 没有在 retry 前重新查询 hook aggregate state

4. runtime maintenance repair dispatch
   - 当前可通过 `handoffWorkflowTaskToAgent(...)` 重新投递 repair / stale handoff
   - 不经过统一 hook freshness check

5. `handoffWorkflowTaskToAgent(...)` / Lobster dispatch backend
   - 当前是 transport layer
   - 不应直接承载质量审核
   - 但它需要能接收上游 hook gate 结果，避免绕过 control-plane gate

6. `createWorkflowReviewRoundHandoff(...)` / `recordWorkflowReviewRoundResults(...)`
   - 当前是 legacy review-round 体系
   - 会产 artifact receipt / failure route
   - 但不是 hook policy 驱动

7. `maybeAdvanceAutoCodeReviewForProject(...)`
   - 当前是 feature-specific code review loop
   - 不走 `workflow-hooks/executor.ts`

8. `maybeAdvanceAutoGateReviewForProject(...)`
   - 当前 submit gate 仍是特殊逻辑
   - 不走 hook policy / runtime hook state

9. `maybeAdvanceAutoModeDiscussionForProject(...)`
   - 当前是 auto-mode risk discussion loop
   - 更像 advisor/review loop，不是 hook

10. 直接调用 `completeWorkflowTaskAndContinue(...)` 的未来路径
    - 当前 `research_workflow.complete_task` 会传 `beforeCompleteHook`
    - 但底层函数本身不强制 hook
    - 如果未来有新调用方不传 hook，会绕过 `before_task_complete`

### 稳定覆盖的目标架构

不要把 hook 判断散落塞进每个函数。
应该新增一层 handoff hook gateway / freshness gateway，让所有 handoff path 共用同一套入口。

建议新增模块：

- `tools/workflow-hooks/handoff-gates.ts`

职责：

1. 组装 handoff 相关 `WorkflowHookPointContext`
2. 调用 `evaluateWorkflowHooksForPoint(...)`
3. 查询 hook aggregate state
4. 判断 handoff intent 是否仍然 fresh
5. 返回统一 gate result

建议导出函数：

```ts
evaluateBeforePrepareHandoffHooks(...)
evaluateBeforeHandoffDeliveryHooks(...)
evaluateBeforeHandoffActivationHooks(...)
evaluateBeforeTaskCompleteHooks(...)
checkHandoffHookFreshness(...)
```

这些函数都不应直接做 delivery 或修改业务产物。
它们只返回：

```ts
{
  allowed: boolean
  hookPoint: string
  aggregateVerdict: "pass" | "revise" | "block" | null
  aggregateStatus: string
  blockingReason: string | null
  aggregateRevisionPacketPath: string | null
}
```

### 新增 hook point 建议

为了覆盖 handoff intent 生命周期，建议补两个明确的 hook point：

1. `before_prepare_handoff`
   - 在 `createStageOwnerHandoffIntent(...)` 前
   - 用来审核“是否应该创建这个 handoff intent”
   - 适合显式 `prepare_stage_handoff` 和 service 主路径共用

2. `before_handoff_delivery`
   - 在 `deliverWorkflowHandoffIntent(...)` 前
   - 用来审核“这个已准备的 intent 是否仍然可以投递”
   - 适合 delivery / sweep / maintenance retry

已有 hook point 继续保留：

- `before_stage_handoff`
  - 用于 stage-level transition gate
- `before_handoff_activation`
  - 用于 owner/stage 切换前的 final safety net
- `after_handoff_activation`
  - 用于非阻塞记录和后处理

### Handoff freshness contract

需要让 handoff intent 知道自己基于哪一版 hook/gate 状态创建。

建议在 handoff payload 或 dedicated fields 中记录：

```json
{
  "hookGateFingerprint": "...",
  "hookGateVerdict": "pass",
  "hookGateCheckedAt": "...",
  "hookGatePoint": "before_stage_handoff",
  "hookGatePolicyIds": ["..."]
}
```

fingerprint 应该由这些内容组成：

- project root / project id
- stage before / stage after
- owner before / owner after
- hook point
- matching hook ids
- latest aggregate verdict
- aggregate revision packet path
- relevant artifact / packet fingerprints

delivery / sweep / retry 时必须重新计算 freshness：

- fresh => 可以继续 delivery
- stale => intent 转 `superseded` 或 `failed`
- revise/block => 生成 revision dispatch 或 route failure

### 显式 `prepare_stage_handoff` 的改造

当前 `prepare_stage_handoff` 是最容易绕过 service 主路径的入口。
稳定覆盖时，它应该改成：

1. 解析 target stage / target role
2. 构造 handoff hook context
3. 跑 `before_prepare_handoff`
4. 跑或复用 `before_stage_handoff`
5. 只有 hook aggregate pass 才允许创建 intent
6. 把 hook gate fingerprint 写进 intent payload
7. delivery 前再跑 `before_handoff_delivery`

这样显式工具路径和自动 service 路径会收敛到同一套质量审核。

### Delivery / sweep / retry 的改造

`deliverWorkflowHandoffIntent(...)` 不应该变成复杂的 reviewer。
但它应该调用一个轻量 freshness gate：

```ts
const gate = await checkHandoffHookFreshness({ intent, hookPoint: "before_handoff_delivery" })
if (!gate.allowed) {
  transition intent -> superseded / failed / escalated
  return delivered=false
}
```

`sweepPendingHandoffIntents(...)` 和 runtime maintenance retry 也应通过同一函数。

这样可以避免：

- old intent 在新 hook revision 后继续投递
- stale owner handoff 绕过最新质量 gate
- delivery retry 把已经被 hook 判定 revise/block 的 work 继续发给下游

### Legacy review loops 的迁移策略

不要一次性删除旧 review loop。
稳定迁移应该采用 built-in hook policy 兼容层：

1. 保留 `maybeAdvanceAutoCodeReviewForProject(...)`
2. 新增等价 built-in hook policy：
   - `code-innovation-review-hook`
   - hook point: `before_stage_handoff`
   - stage: `code`
3. 让旧 loop 只负责 materialize packet 或兼容读取旧 state
4. 新流程由 `workflow-hooks/executor.ts` 聚合 verdict

同理：

- submit gate -> `submit-readiness-hook`
- auto-mode discussion -> `auto-mode-risk-hook`
- review-rounds -> `review-round-file-audit-hook` 或 `review-round-aggregate-hook`

目标不是“马上删旧代码”，而是让新 handoff path 的质量结论最终都来自 hook aggregate。

### Task completion 的稳定覆盖

`completeWorkflowTaskAndContinue(...)` 应从“可传 hook”升级为“默认带 hook gateway”。

建议：

1. `completeWorkflowTaskAndContinue(...)` 内部默认调用 `before_task_complete`
2. 调用方仍可传 override / dependencies 方便测试
3. 如果 hook runtime 不可用，按 policy 决定：
   - blocking hook: task `needs_repair`
   - warn-only hook: 允许完成并记录 warning

这样未来新调用方不会忘记传 `beforeCompleteHook`。

### 可测试的完成定义

完成 handoff hook 稳定覆盖后，应该有这些测试：

1. `prepare_stage_handoff` 被 hook block 时不创建 handoff intent
2. `prepare_stage_handoff` hook pass 时 intent payload 带 hook gate fingerprint
3. `deliverWorkflowHandoffIntent(...)` 遇到 stale hook gate 时拒绝 delivery
4. `sweepPendingHandoffIntents(...)` 不会重投 stale hook-gated handoff
5. runtime maintenance retry 不会绕过 hook freshness
6. `completeWorkflowTaskAndContinue(...)` 直接调用也会跑 default task hook
7. legacy review-round pass/revise/block 可以映射进 hook aggregate

### 这部分实现完成后，handoff 质量覆盖应达到

- 自动 handoff、显式 handoff、retry handoff、activation handoff 都有 hook gate
- task closeout 不依赖调用方“记得传 hook”
- old review loop 不再是平行质量真相源，而是逐步变成 hook policy / packet 的兼容层
- delivery transport 仍保持单纯，但不会投递 stale / blocked handoff

---

### 2. `statistical_evidence`

建议等级：

- top-tier 项目：`block_stage`
- 普通项目：`warn_only`

原因：

- 对 top-tier 主 claim 来说是硬要求
- 对普通项目来说，先继续推进、后续再补统计通常更合理

推荐 hook point：

- `before_stage_handoff`（experiment -> analyze）

### 2.5. `baseline_dataset_coverage`

问题：

- 当前实验经常只在少数 dataset 上测试
- 但 baseline paper 可能在多个 benchmark / dataset 上报告主结果
- 如果我们只覆盖其中一部分，却在论文中声称对 baseline 有整体改进，会被 reviewer 直接质疑 fairness

建议等级：

- exploratory / smoke 阶段：`warn_only`
- ordinary project 主结果阶段：`revise`
- top-tier headline empirical claim：`block_stage`

原因：

- dataset coverage parity 是 baseline fairness 的核心组成部分
- 但早期探索不能因为没跑全 dataset 就卡死
- 真正需要硬卡的是：
  - 准备进入 analyze 的 top-tier 主结果
  - 或即将写入 abstract / intro / conclusion 的强 claim

推荐 hook point：

- `before_stage_handoff`（experiment -> analyze）
- `before_task_complete`（dataset-specific experiment task）

验收原则：

- 如果 baseline 在 N 个核心数据集上报告主结果，我们至少要明确：
  - covered datasets
  - missing datasets
  - intentionally excluded datasets
  - exclusion reason
  - claim wording downgrade

不允许：

- 用 1-2 个 dataset 的结果写成“overall SOTA”
- 用 subset result 支撑 abstract 里的全局 claim
- 忽略 baseline paper 中的 hard dataset

### 3. `ablation_evidence`

建议等级：

- top-tier 方法论文：`revise`
- top-tier 决定性主 claim 且已进入 analyze->review：可升级为 `block_stage`
- survey / 普通项目：`warn_only`

原因：

- ablation sufficiency 常常决定“能否说服 reviewer”，但不是所有项目都值得在 experiment 阶段就硬卡住
- 更适合作为 review pressure 和 revision dispatch 的驱动因素

推荐 hook point：

- `before_stage_handoff`（analyze -> review）

### 4. `mechanism_evidence`

建议等级：

- 默认：`revise`
- 只有当论文主线明确卖机制解释、且 claim wording 很强时，才升级为 `block_stage`

原因：

- 机制证据不足通常不应该阻塞研究继续推进
- 但应该强制触发 reviewer revision packet

推荐 hook point：

- `artifact_materialized`
- `before_stage_handoff`（analyze -> review）

### 5. `venue_competition`

建议等级：

- 默认：`warn_only`
- top-tier 冲刺阶段：`revise`
- 不建议作为硬 block

原因：

- venue competition 主要是竞争定位问题，不是研究正确性问题
- 它应该改变 story 和 scope，不应该中断 auto research

推荐 hook point：

- `artifact_materialized`
- `before_stage_handoff`（analyze -> review 或 review -> write）

### 6. `opportunity_scorecard`

建议等级：

- 永远不直接 block
- 只用于：
  - 决定是否启用更严格 hook policy
  - 决定 stage-preflight 的推荐动作

原因：

- 它本质上是策略判断，不是 correctness gate

### 7. `citation_audit`

建议等级：

- `write` 阶段：`revise`
- `submit` 阶段：`block_stage`

原因：

- citation 问题在 write 期间可以边写边修
- 但 submit 前必须变成硬门

推荐 hook point：

- `before_stage_handoff`（review -> write）
- `before_stage_handoff`（submit）
- `before_handoff_activation`（submit safety net）

### 8. `figure/table registry`

建议等级：

- `write` 阶段：`warn_only`
- `submit` 阶段：`revise`
- unresolved placeholders 到 submit 时：`block_stage`

原因：

- 早期只是写作辅助
- 真正的硬门是 unresolved placeholder，而不是 registry 本身

### 9. `manuscript_lint`

建议等级：

- 普通 lint：`warn_only`
- 一致性冲突（标题/摘要/结论互相冲突、`[analyzer]`、`Table ??`）：`revise`
- submit 前 unresolved hard lint：`block_stage`

原因：

- 大部分 lint 不该阻塞研究推进
- 只有“像拼稿/像未完成稿”的问题才应在临近提交时升级成硬门

### 10. `reproducibility_pack`

建议等级：

- `write` 阶段：`warn_only`
- top-tier `submit` 前：`revise`
- 只有明确目标是 external submission bundle 时才考虑 `block_stage`

原因：

- repro pack 是 submission readiness，不是 research correctness
- 不该阻塞 experiment / analyze

### 11. `camera_ready_evidence`

建议等级：

- `write` 阶段：`warn_only`
- `submit` 阶段：`revise`
- 只有 unresolved placeholders / captions missing / table missing 到最终 handoff 时才 `block_stage`

原因：

- camera-ready 是最典型的“投稿完备性 gate”
- 不能反过来干扰 auto research 主循环

### 12. `final_consistency_audit`

建议等级：

- 只在 `submit` 使用
- `submit` 前：`block_stage`

原因：

- 它就是最终总闸门，不应该提前介入

---

## 二点七、按 hook point 的 severity 策略表

这张表是为了防止实现时误把软审核写成硬阻塞。

### 1. `artifact_materialized`

定位：

- 新 evidence artifact 刚生成后的即时审计点
- 适合发现问题，但不适合频繁阻塞主流程

默认策略：

- 默认 `warn_only`
- 对明显污染事实源的 artifact 可设 `revise`
- 默认不设 `block_stage`

允许 `block_stage` 的少数情况：

- `PROTOCOL_LOCK.json` 明确检测到 protocol drift
- `STATISTICAL_EVIDENCE.json` 结构损坏、无法读取
- `CITATION_AUDIT_REPORT.json` 显示 hallucinated citation 且当前已在 submit stage

不应 block 的情况：

- `VENUE_COMPETITION.json` 竞争定位薄弱
- `MECHANISM_EVIDENCE_PLAN.md` 机制证据还不充分
- `REPRO_PACK.json` 早期不完整
- `CAMERA_READY_EVIDENCE.json` 写作阶段不完整

### 2. `before_stage_handoff`

定位：

- 自动推进主路径上的核心 gate
- 这里可以 block，但必须非常克制

experiment -> analyze：

- `PROTOCOL_LOCK.json`
  - top-tier: `block_stage`
  - ordinary: no extra hook
- `BASELINE_DATASET_COVERAGE.json`
  - exploratory: `warn_only`
  - ordinary: `revise`
  - top-tier headline claim: `block_stage`
- `STATISTICAL_EVIDENCE_SUMMARY.md`
  - top-tier: `block_stage` only for headline empirical claims
  - ordinary: `warn_only`
- `ABLATION_EVIDENCE.json`
  - default: `revise`
  - only upgrade to `block_stage` when the main claim depends on a missing ablation

analyze -> review：

- `MECHANISM_EVIDENCE_PLAN.md`
  - default: `revise`
  - only block if mechanism claim is the paper thesis and evidence is absent
- `VENUE_COMPETITION.json`
  - default: `warn_only`
  - top-tier sprint: `revise`
  - never direct `block_stage`

review -> write：

- `main.tex` / draft claim audit
  - default: `revise`
  - block only if draft contains unsupported strong claims that contradict evidence
- `CITATION_AUDIT_REPORT.json`
  - default: `revise`
  - not block until submit

write -> submit：

- `CITATION_AUDIT_REPORT.json`
  - `block_stage` if hallucinated / unresolved placeholders remain
- `MANUSCRIPT_LINT_REPORT.json`
  - `block_stage` only for hard unresolved placeholders or contradictory method names
- `REPRO_PACK.json`
  - default: `revise`
  - block only for explicit external submission bundle
- `CAMERA_READY_EVIDENCE.json`
  - `revise` by default
  - block only for unresolved figure/table refs at final submit

submit -> done：

- `FINAL_CONSISTENCY_AUDIT.md`
  - `block_stage`

### 3. `before_task_complete`

定位：

- team runtime 的任务关闭前 gate
- 这里不要过度 block，否则 team 会频繁卡死

默认策略：

- correctness task: 可 `block_stage`
- quality task: 优先 `revise`
- presentation task: 优先 `warn_only` 或产生 follow-up task

建议：

- `benchmark_protocol` task:
  - `block_stage`
- `statistical_evidence` task:
  - top-tier empirical claim: `block_stage`
  - otherwise: `revise`
- `mechanism_evidence` task:
  - `revise`
- `venue_competition` task:
  - `warn_only`
- `reproducibility_pack` task:
  - `revise`
- `camera_ready_evidence` task:
  - `revise` only near submit

### 4. `before_handoff_activation`

定位：

- runtime final safety net
- 用来防止非 service 路径绕过 gate

默认策略：

- 只检查硬门
- 不要在这里执行普通质量审查

允许硬门：

- protocol drift
- hallucinated citations at submit
- final consistency audit fail
- unresolved placeholders at submit
- hook aggregate verdict 已经是 `block`

不允许硬门：

- venue fit 弱
- mechanism evidence 不够漂亮
- related work 覆盖还可以更好
- figure aesthetic 不够强

### 5. `after_handoff_activation`

定位：

- post-handoff notification / audit trail
- 不应该阻塞或回滚

默认策略：

- `warn_only`
- 写 dashboard note / reviewer note
- 不做 block

### 6. `before_stage_complete`

定位：

- 目前保留但不是主入口
- 后续可用于阶段 closeout hygiene

建议：

- 默认 `warn_only`
- 只在 submit closeout 时允许升级

---

## 二点八、默认 hook policy 模板建议

### Top-tier empirical paper

硬门：

- protocol lock drift
- statistical evidence missing for headline empirical claim
- hallucinated citations at submit
- final consistency audit fail

软门：

- ablation insufficiency
- mechanism evidence missing/weak
- manuscript claim mismatch
- repro pack incomplete before final submit

提醒：

- venue competition weak
- figure/table polish
- related-work breadth can improve

### Survey / critical analysis paper

硬门：

- fake / placeholder citations at submit
- missing source-to-claim traceability for quantitative synthesis
- final consistency audit fail

软门：

- weak comparability report
- incomplete SOTA matrix
- taxonomy instability
- source maturity ambiguity

提醒：

- venue fit
- coverage could be broader
- open problems could be ranked better

### Ordinary experimental paper

硬门：

- protocol drift if explicit protocol lock exists
- hallucinated citations at submit
- compile / hard manuscript failure

软门：

- weak stats
- missing ablations
- mechanism evidence weak

提醒：

- top-tier opportunity weak
- venue competition thin
- camera-ready polish


---

## 三、第一阶段：先把 evidence materialization 接进 runtime，但只在正确节点触发

### 1. `artifact_materialized` 之前先明确：谁负责生成，谁负责审

#### 需要改造的地方

- `tools/workflow-guard-runtime/stage-preflight.ts`
- `tools/workflow-guard-runtime/auto-iterator.ts`

#### 目标

在关键 stage 边界，先 materialize evidence artifacts，然后通过 hook event 暴露“新产物已生成”。

#### 需要接入的 materializer

experiment 末尾：

- `materializeBenchmarkRegistry`
- `materializeStatisticalEvidence`
- `materializeAblationSufficiency`

analyze 末尾：

- `materializeMechanismEvidence`
- `materializeVenueCompetitionIntel`
- `materializeOpportunityScorecard`

write / submit 前：

- `materializeCitationAudit`
- `materializeReproPack`
- `materializeCameraReadyAudit`
- `materializeFinalConsistencyAudit`

survey line：

- `materializeSurveyAnalysis`

#### 关键要求

- 这些 materializer 运行后，必须生成：
  - `materializedArtifacts`
  - `emittedHookEvents`
- 这样 `artifact_materialized` hook 才能真正命中它们
- 但 **materialization 本身不要阻塞**
  - 失败时写 pending reason
  - 是否阻塞由后续 hook aggregate verdict 决定

#### 验收标准

- stage-preflight 结束后，hook context 里能看到新生成 artifact
- hook executor 可以针对这些 artifact 触发 audit

---

## 四、第二阶段：用 workflow hooks 接管审核流程，而不是再写一套 review loop

### 2. 为 top-tier evidence 生成 hook policy

#### 需要新增

新增一个 policy materializer，例如：

- `tools/research-submit/hook-policy-materializer.ts`
  或
- `tools/research-authoring/hook-policy-materializer.ts`

#### 作用

根据项目状态写入：

- `PROJECT_MANIFEST.json.workflow_hooks`

只在这些情况下启用严格 hook：

- `opportunity_scorecard.verdict = worth_top_tier_bet`
- 或 survey 项目进入 critical-analysis mode

#### 第一批 hook policy 应覆盖的文件

##### experiment -> analyze

- `analyzer/STATISTICAL_EVIDENCE_SUMMARY.md`
- `researcher/ABLATION_EVIDENCE.json`
- `researcher/PROTOCOL_LOCK.json`

建议 blocking mode：

- `PROTOCOL_LOCK.json` -> `block_stage`
- `STATISTICAL_EVIDENCE_SUMMARY.md` -> `block_stage` 仅对 top-tier
- `ABLATION_EVIDENCE.json` -> `revise`

##### analyze -> review

- `researcher/MECHANISM_EVIDENCE_PLAN.md`
- `researcher/VENUE_COMPETITION.json`

建议 blocking mode：

- `MECHANISM_EVIDENCE_PLAN.md` -> `revise`
- `VENUE_COMPETITION.json` -> `warn_only`

##### write -> review / submit

- `academic_writer/paper/main.tex`
- `academic_writer/MANUSCRIPT_LINT_REPORT.json`
- `researcher/CITATION_AUDIT_REPORT.json`
- `submit/REPRO_PACK.json`
- `submit/FINAL_CONSISTENCY_AUDIT.md`

建议 blocking mode：

- `main.tex` claim audit -> `revise`，仅 submit safety net 可升级为 `block_stage`
- `MANUSCRIPT_LINT_REPORT.json` -> `warn_only` 或 `revise`
- `CITATION_AUDIT_REPORT.json` -> write 时 `revise`，submit 时 `block_stage`
- `REPRO_PACK.json` -> `warn_only` 到 `revise`
- `FINAL_CONSISTENCY_AUDIT.md` -> submit 时 `block_stage`

##### survey line

- `academic_writer/SURVEY_COMPARABILITY_REPORT.md`
- `researcher/SOURCE_TO_CLAIM_INDEX.json`

建议 blocking mode：

- `SURVEY_COMPARABILITY_REPORT.md` -> `revise`
- `SOURCE_TO_CLAIM_INDEX.json` -> `revise`

不要因为 survey comparability 的初稿不够强，就在早期硬 block survey pipeline

#### 审核器角色建议

- `reviewer`
  - 通用 reviewer-facing 质量门
- `analyzer`
  - 统计 / mechanism / fair compare
- `cross-reviewer`
  - late-stage critique，仅在最终稿或关键 survey synthesis 上启用

#### 不建议做的事

- 不要先扩展新的 hook type
- 先把 `file_audit` 用到极致

#### 验收标准

- top-tier 项目会自动写出 project-local hook policy
- hook policy 随阶段变化，而不是一次性写死

---

### 3. 把 review 流程改成 hook aggregate verdict 驱动

#### 需要改造的地方

- `tools/workflow-guard-runtime/stage-preflight.ts`
- `tools/workflow-team/task-hooks.ts`
- `tools/workflow-handoff/handoff-activation.ts`

#### 当前建议

统一行为：

- `artifact_materialized`
  - 只做新 evidence artifact 审核
- `before_stage_handoff`
  - 决定能不能进入下一 stage
- `before_task_complete`
  - team task 不再只看 deterministic verification
- `before_handoff_activation`
  - 作为最终兜底 gate

#### 目标

顶级项目的审核流程不要散落在：

- `review-pressure-materializer`
- 某个 auto discussion
- 某个 paper quality eval
- 某个 team verification rule

而是收敛成：

- evidence 生成
- hook 审核
- aggregate verdict
- revision dispatch

#### 聚合策略建议

- 任何 correctness 类 hard gate 失败 -> `block`
- 任何 top-tier quality 类问题 -> 优先 `revise`
- 只有最终 submit closeout 才允许多个 revise 升级成 `block`

#### 验收标准

- 如果 hook verdict = `revise` 或 `block`
  - stage/handoff/task closeout 都会被一致阻止

---

## 五、第三阶段：把 command surface 和 team runtime 接到 hook-aware evidence 流程

### 4. workflow tools 要能显式触发 materialization 与 hook policy refresh

#### 需要新增或重构的 action

- `materialize_benchmark_protocol`
- `materialize_statistical_evidence`
- `materialize_ablation_evidence`
- `materialize_mechanism_evidence`
- `materialize_venue_competition`
- `materialize_opportunity_scorecard`
- `materialize_citation_audit`
- `materialize_repro_pack`
- `materialize_camera_ready_evidence`
- `materialize_final_consistency_audit`
- `materialize_survey_analysis`
- `refresh_workflow_hook_policy`

#### 需要改造

- `tools/register-workflow-tools.ts`
- `tools/workflow-commands.ts`

#### 目标

让 workflow 的外部入口和自动入口都走同一套实现：

- 自动推进
- 手动命令
- team runtime

#### 验收标准

- materializer 不再只在测试中被调用

---

### 5. team runtime 不只消费 verificationRule，还要刷新 hook gate

#### 需要改造

- `tools/workflow-team/task-hooks.ts`
- `tools/workflow-team/stage-profiles.ts`

#### 当前问题

现在 team runtime 已经知道：

- `benchmark_protocol`
- `statistical_evidence`
- `mechanism_evidence`
- `venue_competition`
- `reproducibility_pack`
- `camera_ready_evidence`

但还没有和 workflow hook aggregate state 形成统一闭环。

#### 目标

team 完成 task 前应按下面顺序：

1. 先 materialize 对应 evidence
2. 再运行 `before_task_complete` hook
3. 再看 aggregate verdict

#### 放宽建议

- `before_task_complete` 默认只对 correctness 类 evidence 开硬门
- 对 mechanism / venue / prose / camera-ready 类问题，优先标记：
  - `needs_repair`
  - `revision_requested`
  - 或附加 follow-up task

不要让 team runtime 因为非关键 reviewer 意见反复阻塞主任务图

#### 验收标准

- team 任务关闭时不会绕过 reviewer hook

---

## 六、第四阶段：重写 review-pressure 的职责边界

### 6. `review-pressure-materializer` 不再承担“审核执行器”角色

#### 当前问题

`review-pressure-materializer.ts` 更像：

- 生成 reject-first review 文案
- novelty attack
- unsupported claim audit

这些内容有价值，但不应该继续成为真正的审核控制平面。

#### 新职责

它应该变成：

- reviewer-facing packet generator
- objection summary generator
- supporting artifact aggregator

真正的“是否放行”交给：

- workflow hook aggregate verdict

#### 放宽建议

- review-pressure packet 自身永远不要成为硬门
- 它只能作为 supporting artifact
- 真正的 block 只能来自：
  - hook reviewer 对具体文件的 verdict
  - 或 deterministic stage gate

#### 需要改造

- `tools/workflow-guard-materializers/review-pressure-materializer.ts`

#### 验收标准

- review-pressure packet 变成 hook 的 supporting artifact
- 不再自己模拟独立 gate

---

## 七、第五阶段：让 survey line 成为 hook-aware 的 critical-analysis 流程

### 7. survey pipeline 也走 hook，而不是只走 diagnostics

#### 当前已有

- `tools/survey-review-diagnostics.ts`
- `tools/research-authoring/survey-analysis.ts`

#### 需要新增

为 survey line 自动生成这些 file audit hooks：

- audit `SURVEY_COMPARABILITY_REPORT.md`
- audit `SOURCE_TO_CLAIM_INDEX.json`
- audit `SOTA_MATRIX.md`
- audit `REVIEW_PROTOCOL.md`

#### 为什么

因为 survey 的核心风险现在不是“有没有结构”，而是：

- compare 是否公平
- quantitative claim 是否可回指
- source maturity 是否诚实

这些很适合 `file_audit` 来卡。

#### 验收标准

- survey handoff 到 write 前，comparability/source-traceability 必须先通过 hook

---

## 八、第六阶段：少改 `workflow-guard.ts`，优先通过 hook gateway 和 orchestrator adapter 接入

### 8. 不要把新 top-tier 逻辑重新塞回 monolith

当前 `workflow-guard.ts` 已经很大。

整合策略应该是：

- 新 evidence materializer 写在 `research-*`
- 新审核逻辑走：
  - `workflow-hooks/gateways.ts`
  - `workflow-hooks/executor.ts`
  - `workflow-hooks/state.ts`
- `workflow-guard.ts` 只做 orchestrator wiring

#### 验收标准

- 新增 top-tier 流程不再要求在 `workflow-guard.ts` 里长出一整段 reviewer loop

---

## 九、测试计划也要改成 hook-aware

### 9. 先补这几类测试

#### A. evidence materialization + hook context

新增：

- `tests/top-tier-artifact-materialized-hooks.test.mjs`

验证：

- materializer 运行后
- `artifact_materialized` hook point 能看到正确 artifact/event

#### B. before_stage_handoff hook gate

新增：

- `tests/top-tier-stage-handoff-hook-gates.test.mjs`

验证：

- top-tier 项目在 `before_stage_handoff` 时
- 若 reviewer verdict = `revise/block`
- stage 不推进

#### C. before_task_complete hook gate

新增：

- `tests/team-task-hook-evidence-closeout.test.mjs`

验证：

- team 完成任务前
- evidence 先 materialize
- hook aggregate verdict 再决定 closure

#### D. survey hook-aware analysis line

新增：

- `tests/survey-critical-analysis-hooks.test.mjs`

验证：

- survey artifacts 会触发 file_audit hook
- 不公平 compare 会 block handoff

#### E. final submit hook gate

新增：

- `tests/final-submit-hook-audit.test.mjs`

验证：

- `FINAL_CONSISTENCY_AUDIT.md`
- `CITATION_AUDIT_REPORT.json`
- `CAMERA_READY_EVIDENCE.json`

在 submit handoff 前都可通过 hook 控制

#### F. non-blocking policy tests

新增：

- `tests/top-tier-hook-severity-policy.test.mjs`

验证：

- `venue_competition` 默认不会 block
- `mechanism_evidence` 默认是 revise，不是 block
- `repro_pack` 不会在 experiment / analyze 阶段阻塞
- `camera_ready` 只会在 submit 前成为硬门

---

## 十、推荐实施顺序（hook-aware 版本）

1. 先把 evidence materializer 接入 `stage-preflight` / `auto-iterator`
2. 再实现 top-tier / survey 的 hook policy materializer
3. 接着把 `artifact_materialized` 与 `before_stage_handoff` 打通
4. 再把 `before_task_complete` 接入 team runtime
5. 再重构 review-pressure 角色边界
6. 最后补 survey critical-analysis hooks 和 final submit hooks

在整个过程中都要遵守：

- 先加 `warn_only`
- 再升到 `revise`
- 最后只有必要时再升成 `block_stage`

不要一开始就把所有新审核点当硬门

---

## 十点五、Baseline 数据集覆盖专门优化计划

这是新增的重点问题：每次实验只覆盖少量数据集，导致无法公平对比 baseline 的完整结果。

### 1. 新增一等公民 contract：`baseline_dataset_coverage`

建议新增状态块：

```json
{
  "baseline_dataset_coverage": {
    "schema_version": 1,
    "status": "missing | partial | ready | waived",
    "baseline_reference": "SimGCD",
    "required_datasets": [],
    "covered_datasets": [],
    "missing_datasets": [],
    "waived_datasets": [
      {
        "dataset": "Herbarium19",
        "reason": "not applicable to current compute budget / not in scoped claim"
      }
    ],
    "coverage_ratio": 0.0,
    "claim_scope": "subset | full_baseline_suite",
    "pending_reason": null
  }
}
```

建议落点：

- `tools/research-contracts/evidence-contracts.ts`
- `tools/research-evidence/baseline-dataset-coverage.ts`
- 兼容入口：`tools/workflow-evidence/baseline-dataset-coverage.ts`

### 2. required datasets 从哪里来

按优先级读取：

1. `PROJECT_MANIFEST.json.research_program.datasets`
2. `TRACK_REGISTRY.json.tracks[].datasets`
3. `planner/EXPERIMENT_SEARCH_SPEC.json.dataset_coverage_contract.required_datasets`
4. `researcher/PAPER_IDENTITY_REGISTRY.json` 中 baseline paper 的 benchmark metadata
5. 人工 override / waiver

如果没有完整来源：

- 不要静默通过
- 标记 `status = partial`
- 在 plan / review pressure 里提示“baseline dataset suite unresolved”

### 3. 实验运行时要按 dataset 建 task

当前问题：

- experiment search 往往围绕 method/config 展开
- dataset 维度没有被强制拆成 task graph

建议：

- 在 `workflow-team/stage-profiles.ts` 或 experiment search materializer 中生成 dataset tasks：
  - `experiment.run.dataset.<dataset>.baseline`
  - `experiment.run.dataset.<dataset>.proposed`
  - `experiment.aggregate.dataset.<dataset>`

验收标准：

- baseline core dataset suite 中每个 dataset 都有任务节点
- 没跑的 dataset 必须有 waiver

### 4. 统计聚合必须按 dataset 输出

新增输出：

- `analyzer/DATASET_COVERAGE_MATRIX.json`
- `analyzer/DATASET_COVERAGE_SUMMARY.md`

每行至少包含：

- dataset
- baseline metric
- proposed metric
- delta
- seed count
- protocol lock id
- status: `covered | missing | waived | underseeded`

### 5. claim-evidence 必须绑定 dataset scope

改造：

- `CLAIM_EVIDENCE_AUDIT.json` 中每个 claim 增加：
  - `dataset_scope`
  - `covered_dataset_count`
  - `missing_dataset_count`
  - `claim_scope_status`

规则：

- 如果 claim 写“across benchmarks / overall / consistently”
  - 必须覆盖 baseline core suite
- 如果只覆盖 subset
  - claim 必须写成“on the evaluated subset / on X and Y”

### 6. hook policy 分级

`BASELINE_DATASET_COVERAGE.json` 的建议：

- `artifact_materialized`
  - default: `warn_only`
- `before_stage_handoff` experiment -> analyze
  - exploratory: `warn_only`
  - ordinary: `revise`
  - top-tier headline claim: `block_stage`
- `before_task_complete`
  - dataset task 缺失：`revise`
  - dataset task 声称完成但无结果：`block_stage`
- `before_handoff_activation`
  - 只在 top-tier analyze/review handoff 且 claim scope = `full_baseline_suite` 时硬卡

### 7. 写作阶段的强制降级

如果 dataset coverage 不完整：

- 不阻塞普通写作
- 但必须自动降级措辞

禁止写：

- “consistent improvements across benchmarks”
- “overall SOTA”
- “outperforms baseline broadly”

允许写：

- “on the evaluated subset”
- “on CUB and Cars”
- “preliminary evidence suggests”
- “additional datasets remain unverified”

### 8. 测试计划

新增：

- `tests/baseline-dataset-coverage-contract.test.mjs`
- `tests/baseline-dataset-coverage-hook-policy.test.mjs`
- `tests/claim-dataset-scope-downgrade.test.mjs`
- `tests/experiment-dataset-task-graph.test.mjs`

必须验证：

- baseline 有 5 个 dataset，只跑 2 个时：
  - exploratory 不 block
  - top-tier headline claim block
  - ordinary project revise
- waived dataset 不计入 missing，但必须有 reason
- incomplete coverage 会触发 claim wording downgrade


---

## 十一、完成定义（按当前架构）

这份整合真正完成时，应满足：

1. top-tier evidence 由 runtime 自动 materialize
2. reviewer 审核流程由 workflow hooks 控制，不再散落在 feature-specific 逻辑里
3. handoff / task complete / submit gate 都消费 hook aggregate verdict
4. survey line 和 full paper line 都能用同一套 durable hook 机制做审核闭环
5. 普通项目不受 top-tier hook 误伤
6. 非关键质量问题默认不会阻塞 auto research 主循环

---

## 十二、最先动手的三个文件

如果下一步要开始实施，优先从这里动：

- `tools/workflow-guard-runtime/stage-preflight.ts`
- `tools/workflow-hooks/gateways.ts`
- `tools/workflow-team/task-hooks.ts`

原因：

- 它们分别控制：
  - 产物刚 materialize 时的入口
  - hook 执行入口
  - task closeout 的最终兜底

这是把新 evidence 架构真正整合进 workflow 的最稳定起点。

---

## 十三、把论文 research 流程接进 auto-research workflow（hook-aware 版本）

目标：
把“关键词 / 论述 -> 多源论文召回 -> canonical merge -> PAPER_SOURCE_INDEX -> coverage audit -> citation expansion -> graph-build”这条 research 流程，真正接进当前 `auto-research` 主线里，并且优先复用已经落地的 `workflow hooks` 控制平面。

当前现实约束：

- 当前分支里还没有完整的 broad paper search implementation
- 现有可复用入口主要是：
  - `tools/research30/bridge.ts`
  - `skills/researcher/research-lit/SKILL.md`
  - `tools/paper-discovery-diagnostics.ts`
  - `tools/workflow-hooks/*`
- 当前 workflow hooks 稳定支持：
  - `artifact_materialized`
  - `before_stage_handoff`
  - `before_task_complete`
  - `before_handoff_activation`
  - `after_handoff_activation`
- 当前稳定 hook type 只有：
  - `file_audit`

这意味着：

- 不要先发明新的 hook type
- 不要把 research 审核逻辑塞回 `auto-research` 的 feature-specific loop
- 应该先把 research 流程做成 workflow-owned artifact + hook-aware gate

### 当前代码证据快照

这部分不是设计假设，而是当前仓库里已经存在、可以直接复用的入口：

- `auto-research` 当前确实是 workflow bootstrap 入口
  - 证据：
    - `tools/workflow-commands.ts`
    - `tests/workflow-commands.test.mjs`
    - `tests/auto-command-end-to-end.test.mjs`
- `research-lit` 已经把 literature diagnostics 和 bounded citation expansion 接进 researcher 流程
  - 证据：
    - `skills/researcher/research-lit/SKILL.md`
    - 已明确提到：
      - `research_workflow.audit_literature_coverage`
      - `research_workflow.plan_citation_expansion`
- workflow hooks 当前已经有稳定 command/tool surface
  - 证据：
    - `docs/reference/commands-and-tools.md`
    - `tools/workflow-hooks/state.ts`
    - `tools/workflow-hooks/executor.ts`
  - 当前可直接用的动作：
    - `get_file_audit_state`
    - `set_file_audit_policy`
    - `materialize_file_audit_packet`
- workflow control plane 文档已经明确了 hook 在 stage 推进里的位置
  - 证据：
    - `docs/architecture/workflow-control-plane.md`
    - 已明确顺序：
      1. `stage-preflight` materialize
      2. `artifact_materialized`
      3. `auto_iterator_tick`
      4. `before_stage_handoff`
      5. `before_handoff_activation`
- 当前还没有完成的点也已被验证
  - broad literature search 还没有作为独立 workflow-owned backbone 注册成现成 action
  - 当前 `tools/register-workflow-tools.ts` 已注册：
    - `audit_literature_coverage`
    - `plan_citation_expansion`
    - `run_idea_catalyst_research30`
  - 但还没有 `run_broad_paper_search`

所以这份计划的真正边界是：

- 不是“从零设计 hooks”
- 而是“把尚未 workflow-owned 的 research 检索骨架，接到已经存在的 hook-aware control plane 上”

### 先给结论

最稳定的接法不是：

- 在 `/auto-research` 里直接插几条“搜论文 -> 下载 -> 继续”的 ad hoc 命令
- 或者在 `research-lit` skill prompt 里堆更多口头要求

最稳定的接法应该是：

1. 先把论文 research 做成一条 repo-owned retrieval backbone
2. 让 `research_pipeline` / `auto-research` 在正确阶段显式调用它
3. 让 hooks 只负责审查、回修、放行
4. 让 `graph_build / frontier_mapping / idea -> plan` 的 handoff 统一消费 hook aggregate verdict

---

## 十四、第一阶段：先把 research 流程变成 workflow-owned backbone，而不是 skill prompt

### 1. 当前 research 流程的主要缺口

当前 `auto-research` 里的 literature/research 仍然偏：

- `papers-cool`
- 可选 `pasa-paper-search`
- arXiv markdown/PDF fallback

这条线对 AI/arXiv 友好，但对下面这些情况不够稳：

- 只有模糊 topic 或相关论述
- 顶会/顶刊 paper 主要通过 DOI / metadata 被发现
- 需要保留 metadata-only canonical papers
- 需要系统化地把 coverage audit / citation expansion 串进下一阶段

### 2. 第一批要落成的 workflow-owned artifact

建议的 research artifact contract：

- `researcher/search_plans/<ts>_query_plan.json`
- `researcher/search_raw/<ts>_provider_results.json`
- `researcher/search_merged/<ts>_merged_candidates.json`
- `researcher/search_reports/<ts>_broad_search_report.md`
- `researcher/search_reports/<ts>_metadata_only_unresolved.json`
- `researcher/LITERATURE_COVERAGE_AUDIT.json`
- `researcher/LITERATURE_COVERAGE_AUDIT.md`
- `researcher/CITATION_EXPANSION_PACKET.json`
- `researcher/CITATION_EXPANSION_PACKET.md`
- `researcher/PAPER_SOURCE_INDEX.json`

### 3. 第一批需要新增或重构的代码模块

优先新增：

- `tools/research30/query-planner.ts`
- `tools/research30/provider-contract.ts`
- `tools/research30/provider-openalex.ts`
- `tools/research30/provider-semanticscholar.ts`
- `tools/research30/provider-crossref.ts`
- `tools/research30/provider-unpaywall.ts`
- `tools/research30/provider-dblp.ts`
- `tools/research30/merge.ts`
- `tools/research30/diagnostics.ts`
- `tools/research30/source-resolution.ts`
- `tools/research30/workflow-bridge.ts`

优先重构：

- `tools/research30/bridge.ts`
  - 不再只服务 IDEA-CATALYST
  - 改成可复用的 workflow bridge / compatibility layer
- `tools/paper-discovery-diagnostics.ts`
  - 扩展 top-tier venue / metadata-only / provider diversity 维度
- `tools/paper-source-index.ts`
  - 只做读/兼容 facade，不再暗含第二套 canonical logic

优先新增共享层：

- `tools/paper-source-contract.ts`
- `tools/paper-source-index-writer.ts`

### 4. 第一批 workflow action

需要新增的 `research_workflow` action：

- `run_broad_paper_search`
- `upsert_paper_source_index`（如果 writer 不直接内聚在前者里）
- `refresh_research_coverage_audit`
- `refresh_citation_expansion_packet`
- `refresh_research_hook_policy`

验收标准：

- `auto-research` 和手动命令最终共用同一实现
- 不是一套 command 逻辑、一套 auto path 逻辑

---

## 十五、第二阶段：把 research 流程接进 auto-research 主阶段机

### 5. research 流程应该挂在哪些 stage

建议把 literature/research backbone 接入这些节点：

#### `setup -> graph_build`

目标：

- topic-only bootstrap 后，不要立即把 graph_build 当成“只检查有没有 graph”
- 应先确保项目已经做过第一轮 broad literature search

需要新增的判断：

- 如果 `PAPER_SOURCE_INDEX.json` 为空
- 且还没有 broad search artifact
- 那么 `setup` 结束后不是直接 handoff 到普通 graph_build
- 而是先触发 literature retrieval bootstrap

#### `graph_build`

目标：

- `graph_build` 不只是检查 shared graph
- 还应负责消费最新的 canonical paper 集合

需要新增的判断：

- 如果 coverage audit = `thin`
- 或 baseline hints still missing
- 或 metadata-only unresolved 的关键 paper 过多
- 那么 graph_build 不应立刻宣告 ready-for-frontier

#### `frontier_mapping -> idea`

目标：

- frontier / idea 不应该建立在薄 literature set 上

需要新增的判断：

- 如果最新 literature batch 没经过 coverage audit
- 或 citation expansion packet 尚未执行 / 尚未被明确接受跳过
- 则 frontier mapping 不应宣告真正完成

#### `idea -> plan`

目标：

- ideation handoff 给 orchestrator 前，research 基础必须足够扎实

需要新增的判断：

- `PAPER_SOURCE_INDEX.json` 已形成 canonical set
- coverage audit 不是 `thin`
- 或者 `thin` 的理由已被显式接受并记录

---

## 十六、第三阶段：用 hooks 审 research 流程，而不是把审核塞回 researcher loop

### 6. `artifact_materialized`：审“新生成的 research artifact”

这是最适合先接入的 hook 点。

触发时机：

- broad search query plan 写出后
- merged candidate artifact 写出后
- `PAPER_SOURCE_INDEX.json` 更新后
- coverage audit / citation expansion packet 写出后

第一批适合挂 `file_audit` 的对象：

- `researcher/search_reports/*_broad_search_report.md`
- `researcher/LITERATURE_COVERAGE_AUDIT.md`
- `researcher/CITATION_EXPANSION_PACKET.md`
- `researcher/LITERATURE.md`
- `researcher/RESEARCH_BRAINSTORM.md`

审核目标：

- query 是否过窄
- top-tier venue coverage 是否被明显漏掉
- audit recommendation 是否被正确写出
- brainstorm 是否真的建立在 literature 上

### 7. `before_stage_handoff`：审“Researcher 是否真的可以交棒”

这是 research 流程最关键的 hook 点。

建议先接这些 handoff：

- `researcher(graph_build) -> researcher(frontier_mapping)` 的 stage 推进前
- `researcher(frontier_mapping) -> researcher(idea)` 的 stage 推进前
- `researcher(idea) -> orchestrator(plan)` 的 owner handoff 前

第一批适合挂的 file audit：

- `researcher/PAPER_SOURCE_INDEX.json`
- `researcher/LITERATURE_COVERAGE_AUDIT.json`
- `researcher/CITATION_EXPANSION_PACKET.json`
- `researcher/LITERATURE.md`
- `researcher/RESEARCH_BRAINSTORM.md`

审核目标：

- canonical paper set 是否真实存在
- 是否仍明显缺 baseline / closest prior work
- 是否遗漏了 metadata-only 但关键的顶会/顶刊 paper
- 是否在 paper pool 仍然过薄时就准备 handoff 到 plan

### 8. `before_task_complete`：审 team runtime 的 literature subtasks

如果 team runtime 或 future background worker 把文献检索拆成任务：

- `run broad search`
- `merge canonical papers`
- `resolve OA / PDF`
- `refresh coverage audit`

则 task closeout 前应该统一执行：

1. 先 materialize 最新 research artifact
2. 再跑 `before_task_complete` hook
3. 再决定 task 是否算完成

这样 team runtime 不会绕过 reviewer-style audit。

### 9. `before_handoff_activation`：作为 researcher handoff 的 final safety net

目标：

- 防止 `prepare_stage_handoff` 已经生成，但 literature 实际上还没准备好

适合做的事：

- 对 `idea -> plan` handoff 做最终兜底检查
- 如果 hook aggregate verdict = `block`
  - 则不允许 manifest 真正切换 owner/stage

---

## 十七、第四阶段：自动为 research 流程物化 hook policy

### 10. 不要手写每个项目的 hook policy

最稳定的做法是新增一个 policy materializer，例如：

- `tools/research30/hook-policy-materializer.ts`

它根据项目状态自动写入：

- `PROJECT_MANIFEST.json.workflow_hooks`

### 11. 什么时候自动开启 research hooks

建议条件：

- 项目启动了 `auto-research`
- 当前 topic 不是明显的“只靠 arXiv 几篇 paper 就够”的窄 topic
- 或项目已经进入 `frontier_mapping / idea / plan`，需要更严格 literature grounding

### 12. 第一批 hook policy 模板

#### A. coverage audit gate

- hook point: `before_stage_handoff`
- audited file: `researcher/LITERATURE_COVERAGE_AUDIT.md`
- target role: `researcher`
- auditor role: `reviewer`

#### B. canonical source index gate

- hook point: `before_stage_handoff`
- audited file: `researcher/PAPER_SOURCE_INDEX.json`
- target role: `researcher`
- auditor role: `reviewer`

#### C. brainstorm groundedness gate

- hook point: `artifact_materialized`
- audited file: `researcher/RESEARCH_BRAINSTORM.md`
- target role: `researcher`
- auditor role: `reviewer`

#### D. literature report groundedness gate

- hook point: `before_stage_handoff`
- audited file: `researcher/LITERATURE.md`
- target role: `researcher`
- auditor role: `reviewer`

---

## 十八、第五阶段：把 `auto-research` 命令和 `research-pipeline` 真正串起来

### 13. 当前 `/auto-research` 的缺口

当前 `/auto-research` 更像：

- 建项目
- 写 onboarding placeholder
- 背景启动主流程

但它还没有把“广覆盖论文 research”提升成 startup invariant。

### 14. 建议的串法

#### bootstrap 后第一件事

在：

- `/auto-research`
- `/research-pipeline`
- `/resume-pipeline`

里统一约束：

- 如果项目还没有 canonical paper set
- 优先调用 `run_broad_paper_search`
- 然后再进入传统 `research-lit` 的全文抓取 / graph-build 逻辑

#### broad search 与 `research-lit` 的关系

建议分工：

- `run_broad_paper_search`
  - 负责 breadth
  - 负责 merged candidates / metadata-only retention / coverage audit
- `research-lit`
  - 负责全文 staging
  - 负责 markdown-first / PDF fallback
  - 负责最终 graph-build-ready paper source tree

这样 `research-lit` 就不需要自己承担“如何做 broad recall”这件事。

### 15. 如果 broad search 还没完全实现，临时过渡怎么做

在过渡阶段可以先：

- 用现有 `research30/bridge.ts`
- 结合 `papers-cool + PASA`
- 先产出 query plan / merged search artifact / PAPER_SOURCE_INDEX snapshot

但要坚持：

- artifact shape 先固定
- hooks 先接上
- 后面 provider 扩容时不再改 workflow 语义

---

## 十九、测试计划（research + hooks 版本）

### 16. 必须补的测试

#### A. `auto-research` 启动 literature bootstrap

新增：

- `tests/auto-research-literature-bootstrap.test.mjs`

验证：

- topic-only 项目启动后
- 在 graph-sensitive 阶段前会先产生 research artifact

#### B. `artifact_materialized` research hooks

新增：

- `tests/research-artifact-hooks.test.mjs`

验证：

- broad search / coverage audit / citation expansion 生成后
- hook context 能看到这些 artifact

#### C. `before_stage_handoff` literature gate

新增：

- `tests/research-stage-handoff-hooks.test.mjs`

验证：

- coverage 过薄、baseline 缺失、canonical paper set 不完整时
- `idea -> plan` 或 `graph_build -> frontier_mapping` 不能推进

#### D. team runtime literature task gate

新增：

- `tests/research-task-complete-hooks.test.mjs`

验证：

- team runtime 做 literature subtasks 时
- task 完成前会经过 hook aggregate verdict

---

## 二十、推荐实施顺序（research 接 auto-research 版本）

1. 先把 broad search / canonical merge / source index writer 做成 workflow-owned backbone
2. 再让 `auto-research` / `research-pipeline` 在 setup-early graph 阶段调用它
3. 再物化 research hook policy
4. 先接 `artifact_materialized`
5. 再接 `before_stage_handoff`
6. 最后把 `before_task_complete` / `before_handoff_activation` 接到 team runtime 和 final researcher handoff

---

## 二十一、完成定义（research 流程接入版）

完成后系统应该满足：

1. `auto-research` 启动后，literature bootstrap 不再只是 prompt 约定，而是 workflow-owned artifact 流程
2. broad literature search、coverage audit、citation expansion 能被 durable 落盘
3. hooks 会在正确节点审核这些 research artifact
4. `graph_build / frontier_mapping / idea -> plan` 不会在 literature 明显不足时继续推进
5. 研究检索、team runtime、handoff gate 共用同一套 hook aggregate verdict
6. 普通项目仍可保持轻量，不会被过度严格的 literature hook 全局误伤
